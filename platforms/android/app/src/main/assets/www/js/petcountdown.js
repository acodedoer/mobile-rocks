var petcountdown = new Phaser.Class({

    Extends: Phaser.Scene,

    initialize:

    function petcountdown ()
    {
        Phaser.Scene.call(this, { key: 'petcountdown' });
    },

    create: function ()
    {
        
    }
})